var app = require('./lib/app');

app.on('before', function () {

  app.use(express.cookieParser(app.set('session secret')));
  app.use(memcachedHop);

  // app.get(['/pro'], function (req, res, next) {
  //   res.end('Welcome to pro features');
  // });
});

app.connect();